{
  "name": "cookingbot",
  "version": "1.0.0",
  "description": "AI cookingBot gives cooking recipes with openAI API",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider && webpack",
    "build:digitalocean": "npm install --production=false && npm run build && npm ci"
  },
  "author": "omerdn",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.82.1",
    "webpack-cli": "^5.1.1"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.1.3"
  }
}
